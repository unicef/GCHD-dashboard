.venv/
app/data/

# Secrets — never commit
scripts/.env
app/credentials
app/credentials/
cookie_jar.json

# Generated / local
__pycache__/
*.pyc
scripts/iso3_mwater_review.csv

# Excel lock files (created when a workbook is open)
~$*.xlsx
# El Nino working data — GRIBs and GeoTIFFs, hundreds of MB, all re-downloadable
scripts/el_nino/data/
